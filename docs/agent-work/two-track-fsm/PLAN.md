# Two black tracks with AprilTag/FSM junction decisions
## Approved intent and architecture
Preserve existing eleven 4x3x4 ft gloveboxes, 1 ft corridors, Raspbot model, IR primary/fallback, doors and internal wafer processing. Replace repeated raised U detours by continuous straight black main (Track 1, farther from doors) and service (Track 2, beside doors) lanes, connected by black transverse junctions outside door/box envelopes. Screenshot red strokes mark obsolete detours, not desired red tape. No red path. Keep existing main/service y coordinates where clearance permits; remove redundant third return lane, return on Track 1 after a measured turn at end. Keep 1 ft lane widths. World tape is a network, NOT a fixed all-room trajectory.
FSM owns active route segment/edge and decision. /cmd_vel sole owner remains transport_controller. Sensor reflects all physical black tape; junction steering follows selected edge with bounded explicit junction state, returning to IR steering on straight lane. Pose-based simulated fallback may steer selected edge but cannot fabricate AprilTag authorization. No nearest-projection jumps across parallel lanes. Stop at pre-junction decision point if fresh expected confirmed tag not present, then fault on timeout; wrong room/state tags ignored. Authorization is bounded to current room/decision, never a permanent global set. Entry tag plus entry FSM authorizes Track1->Track2. Exit tag plus completed/closed-door service FSM authorizes Track2->Track1; return-home FSM stays on Track1 and ignores service tags. Stop position uses measured pose/odom, not tag appearance.
Maintain existing tag ID/topic compatibility (22 entry/exit tags). Position markers for actual forward-camera visibility BEFORE branch decision; choose safe straight approach distances, marker size/orientation based on camera geometry. Repeated decoding remains in detector. Do not silently treat geometry as decoded tags.
Use transient-local /transport/track (TRACK_1 or TRACK_2) and /transport/route_segment strings for diagnostics; explicit junction/wait FSM states. Preserve public state/topic consumers or update tests/checker coherently. Door stops and attachment interlocks remain intact.
## Phase and worker scope
One end-to-end bundle: layout/network geometry, world generator/assets, IR network sampling, controller FSM/tag freshness/selected-edge navigation, configuration/checker/tests, README and release ZIP. Existing dirty work is baseline at /tmp/wafer-two-track-baseline-20260923, including prior process reliability changes awaiting broader runtime validation. Worker owns those files only, plus new local route helper/test/preview files as needed; root owns this PLAN and CHECKPOINT. No reset, auto-commit, push or router modifications.
## Acceptance
Static overhead preview shows exactly two continuous black lanes and transverse connections, no per-door U bumps/third lane. Eleven box dimensions preserved. Geometry tests cover continuity and robot corridor/door clearance. Tests exercise FSM same-tag different-state decisions, wrong/stale/missing tags, one-shot authorization, full eleven-room mission and reverse main-lane return without projection discontinuities, IR and fallback selected-edge behavior, zero motion during doors/fault. Test rendered marker projection or document visibility limits; raw PNG decode alone is insufficient visibility evidence. Run existing full suite once after focused tests and resolve genuine failures without weakening checks. Regenerate assets reproducibly, check ZIP installed assets and distinct marker. Create top-view PNG from actual generated geometry for root visual review. No ROS/Gazebo locally; distinguish static/unit evidence from unexecuted Ubuntu camera/physics validation.
## Review
Root performs one batched delta/spec/quality review and targeted missing-evidence checks, then at most one consolidated correction. Physical VM validation remains outstanding and must be stated honestly.
